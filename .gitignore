# ---- build output ----
/target
kernel/target/
user/target/
Cargo.lock
kernel/Cargo.lock
user/Cargo.lock

# ---- generated, reproducible ----
# lebos.img   -> make disk
# qemu.log    -> make trace
# virt.dtb/s  -> make dumpdtb
lebos.img
qemu.log
virt.dtb
virt.dts

# ---- scratch copies of the reference kernel ----
# Deliberately untracked so they can be broken freely; `make resync` recreates
# them. Note the paths: these moved from src/ to reference/ and the old
# patterns silently stopped matching, which committed 3500 lines of duplicate.
reference/main2.rs
reference/entry2.S
reference/banner_art.txt

# learn/ IS tracked. It was ignored as "notes rather than source", which was
# wrong: it is ~1400 lines of documentation about this project and it existed
# in exactly one place on one disk.

# ---- editors ----
.idea/
