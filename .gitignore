/target
Cargo.lock
.idea/

# Scratch copy of main.rs for experiments -- deliberately untracked so it can
# be broken freely. `make resync` recreates it from main.rs.
src/main2.rs

# Scratch pad for working out kernel logic in Python before translating.
questions.py
qemu.log
virt.dtb
virt.dts
